# 속담 게임 - 통합 시스템 패키지 목록
# 난이도 측정 + 유사도 측정 기능 통합 버전
# 설치 명령어: pip install -r requirements.txt

# ==================== 웹 프레임워크 ====================
fastapi==0.104.1              # FastAPI - 웹 API 서버
uvicorn[standard]==0.24.0     # ASGI 서버 - API 서버 실행
pydantic==2.5.0               # 데이터 검증 - API 입출력 검증
python-multipart==0.0.6       # 파일 업로드 지원
jinja2==3.1.2                 # 템플릿 엔진 - HTML 렌더링

# ==================== 데이터베이스 ====================
sqlalchemy==2.0.23            # ORM - 데이터베이스 모델링
mysql-connector-python==8.2.0 # MySQL 연결 - 속담 DB 접근
pymysql==1.1.0                # MySQL 클라이언트 (대체 연결)
alembic==1.13.1               # 데이터베이스 마이그레이션

# ==================== AI/ML 핵심 라이브러리 ====================
# PyTorch 생태계 (난이도 측정용)
torch==2.1.1                  # PyTorch - AI 모델 실행 엔진
transformers==4.35.2          # Hugging Face Transformers - BERT 기반 모델
huggingface_hub==0.16.4       # Hugging Face 모델 다운로드

# TensorFlow 생태계 (유사도 측정용)
tensorflow==2.15.0            # TensorFlow - 딥러닝 프레임워크

# 문장 임베딩 및 유사도 계산
sentence-transformers==2.2.2  # 문장 임베딩 생성 및 속담 유사도 계산
scikit-learn==1.3.2           # 머신러닝 도구 - 난이도 분류 및 유사도 측정
faiss-cpu==1.7.4              # 벡터 유사도 검색 (유사도 측정용)

# 자연어 처리
nltk==3.8.1                   # 자연어 처리 도구

# ==================== 데이터 처리 ====================
pandas==2.1.4                 # 데이터 프레임 - 속담 데이터 관리
numpy==1.25.2                 # 수치 연산 - 임베딩 벡터 처리

# ==================== 캐싱 및 큐 ====================
redis==5.0.1                  # Redis - 캐싱 및 세션 저장
celery==5.3.4                 # 비동기 작업 큐

# ==================== HTTP 클라이언트 ====================
httpx==0.25.2                 # 비동기 HTTP 클라이언트
requests==2.31.0              # 동기 HTTP 클라이언트

# ==================== 시스템 유틸리티 ====================
python-dotenv==1.0.0          # 환경 변수 관리
psutil==5.9.0                 # 시스템 모니터링 - 메모리 사용량 추적
tqdm==4.65.0                  # 진행률 표시 - 모델 로딩 진행상황

# ==================== 보안 및 인증 ====================
python-jose[cryptography]==3.3.0  # JWT 토큰 처리
passlib[bcrypt]==1.7.4            # 비밀번호 해싱

# ==================== 개발 및 테스트 도구 ====================
pytest==7.4.3                 # 테스트 프레임워크
pytest-asyncio==0.21.1        # 비동기 테스트
black==23.11.0                 # 코드 포맷터
flake8==6.1.0                  # 코드 린터
mypy==1.7.1                    # 타입 체커

# ==================== 모니터링 ====================
prometheus-client==0.19.0     # 메트릭 수집 및 모니터링